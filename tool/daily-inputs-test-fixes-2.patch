diff --git a/test/daily_inputs_lab_test.dart b/test/daily_inputs_lab_test.dart
--- a/test/daily_inputs_lab_test.dart
+++ b/test/daily_inputs_lab_test.dart
@@ -224,10 +224,8 @@ void main() {
     await tester.pumpAndSettle();
 
     expect(find.text('DAILY INPUTS & RECOVERY'), findsOneWidget);
-    await tester.scrollUntilVisible(find.text('Today at a glance'), 300);
-    await tester.pumpAndSettle();
-    expect(find.text('Today at a glance'), findsOneWidget);
-    expect(find.text('Quick add'), findsOneWidget);
+    expect(find.text('TODAY AT A GLANCE'), findsOneWidget);
+    expect(find.text('QUICK ADD'), findsOneWidget);
 
     await tester.pumpWidget(
       MaterialApp(
diff --git a/test/navigation_widget_test.dart b/test/navigation_widget_test.dart
--- a/test/navigation_widget_test.dart
+++ b/test/navigation_widget_test.dart
@@ -148,13 +148,13 @@ void main() {
     await tester.pumpAndSettle();
 
     expect(find.text('Barbell Bench Press'), findsWidgets);
+    await tester.tap(find.text('Weight'));
+    await tester.pumpAndSettle();
+    expect(find.text('WORKING WEIGHT'), findsOneWidget);
+    expect(find.byType(CustomPaint), findsWidgets);
     await tester.scrollUntilVisible(find.textContaining('185 lb × 6'), 300);
     await tester.pumpAndSettle();
     expect(find.textContaining('185 lb × 6'), findsWidgets);
-    await tester.tap(find.text('Weight'));
-    await tester.pumpAndSettle();
-    expect(find.text('WORKING WEIGHT'), findsOneWidget);
-    expect(find.byType(CustomPaint), findsWidgets);
   });
 
   testWidgets('logged set edits require the explicit save button', (

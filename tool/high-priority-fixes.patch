diff --git a/lib/app_tour.dart b/lib/app_tour.dart
index 7e10366..a6a4424 100644
--- a/lib/app_tour.dart
+++ b/lib/app_tour.dart
@@ -301,14 +301,18 @@ class _TourFactCard extends StatelessWidget {
             ),
           ),
           const SizedBox(height: 8),
-          Text(
-            step.body,
-            style: const TextStyle(
-              color: BrandColors.muted,
-              height: 1.42,
+          Expanded(
+            child: SingleChildScrollView(
+              child: Text(
+                step.body,
+                style: const TextStyle(
+                  color: BrandColors.muted,
+                  height: 1.42,
+                ),
+              ),
             ),
           ),
-          const Spacer(),
+          const SizedBox(height: 10),
           Row(
             children: [
               if (onBack != null)
diff --git a/lib/main.dart b/lib/main.dart
index 9ebc996..2ec3fc0 100644
--- a/lib/main.dart
+++ b/lib/main.dart
@@ -2012,28 +2012,31 @@ class _Setting extends StatelessWidget {
   final String? badge;
   final VoidCallback? onTap;
   @override
-  Widget build(BuildContext context) => ListTile(
-    onTap: onTap,
-    contentPadding: EdgeInsets.zero,
-    leading: CircleAvatar(
-      backgroundColor: Colors.white10,
-      child: Icon(icon, color: available ? cyan : Colors.white38),
-    ),
-    title: Text(title),
-    subtitle: Text(subtitle),
-    trailing: Container(
-      padding: const EdgeInsets.symmetric(horizontal: 8, vertical: 4),
-      decoration: BoxDecoration(
-        color: (available ? lime : Colors.white38).withValues(alpha: .1),
-        borderRadius: BorderRadius.circular(99),
+  Widget build(BuildContext context) => Material(
+    color: Colors.transparent,
+    child: ListTile(
+      onTap: onTap,
+      contentPadding: EdgeInsets.zero,
+      leading: CircleAvatar(
+        backgroundColor: Colors.white10,
+        child: Icon(icon, color: available ? cyan : Colors.white38),
       ),
-      child: Text(
-        badge ?? (available ? 'ACTIVE' : 'PLANNED'),
-        style: TextStyle(
-          color: available ? lime : Colors.white38,
-          fontSize: 9,
-          fontWeight: FontWeight.w900,
-          letterSpacing: .8,
+      title: Text(title),
+      subtitle: Text(subtitle),
+      trailing: Container(
+        padding: const EdgeInsets.symmetric(horizontal: 8, vertical: 4),
+        decoration: BoxDecoration(
+          color: (available ? lime : Colors.white38).withValues(alpha: .1),
+          borderRadius: BorderRadius.circular(99),
+        ),
+        child: Text(
+          badge ?? (available ? 'ACTIVE' : 'PLANNED'),
+          style: TextStyle(
+            color: available ? lime : Colors.white38,
+            fontSize: 9,
+            fontWeight: FontWeight.w900,
+            letterSpacing: .8,
+          ),
         ),
       ),
     ),
diff --git a/lib/share_card.dart b/lib/share_card.dart
index ec4445b..378abbb 100644
--- a/lib/share_card.dart
+++ b/lib/share_card.dart
@@ -1,4 +1,3 @@
-import 'dart:typed_data';
 import 'dart:ui' as ui;
 
 import 'package:flutter/material.dart';
@@ -175,6 +174,7 @@ class WorkoutShareCardGenerator {
             BrandColors.magenta.withValues(alpha: .36),
             BrandColors.panelHigh.withValues(alpha: .96),
           ],
+          const [0, .52, 1],
         ),
     );
     canvas.drawRRect(
@@ -402,6 +402,7 @@ class WorkoutShareCardGenerator {
           rect.topLeft,
           rect.bottomRight,
           const [BrandColors.cyan, BrandColors.violet, BrandColors.magenta],
+          const [0, .5, 1],
         ),
     );
     final flask = Path()
diff --git a/test/navigation_widget_test.dart b/test/navigation_widget_test.dart
index 52ba5d8..9f9d2a1 100644
--- a/test/navigation_widget_test.dart
+++ b/test/navigation_widget_test.dart
@@ -395,6 +395,11 @@ void main() {
   testWidgets('main navigation groups programs and keeps the tour replayable', (
     tester,
   ) async {
+    tester.view.physicalSize = const Size(430, 900);
+    tester.view.devicePixelRatio = 1;
+    addTearDown(tester.view.resetPhysicalSize);
+    addTearDown(tester.view.resetDevicePixelRatio);
+
     final store = AppStore()
       ..isLoaded = true
       ..onboardingVersionSeen = 1;
@@ -404,7 +409,8 @@ void main() {
     );
     await tester.pumpAndSettle();
 
-    expect(find.byType(NavigationDestination), findsNWidgets(4));
+    final navigation = tester.widget<NavigationBar>(find.byType(NavigationBar));
+    expect(navigation.destinations, hasLength(4));
     expect(find.text('Home'), findsOneWidget);
     expect(find.text('Programs'), findsOneWidget);
     expect(find.text('Progress'), findsOneWidget);
@@ -447,9 +453,12 @@ void main() {
       highlightValue: 'Barbell Bench Press · 185 lb × 6',
     );
 
-    final bytes = await WorkoutShareCardGenerator.generate(data);
+    final bytes = await tester.runAsync(
+      () => WorkoutShareCardGenerator.generate(data),
+    );
 
-    expect(bytes.length, greaterThan(10000));
+    expect(bytes, isNotNull);
+    expect(bytes!.length, greaterThan(10000));
     expect(bytes.take(8).toList(), [137, 80, 78, 71, 13, 10, 26, 10]);
     expect(data.fileName, 'progression-lab-20260819-upper-body-a.png');
   });

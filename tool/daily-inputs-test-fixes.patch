diff --git a/test/program_test.dart b/test/program_test.dart
--- a/test/program_test.dart
+++ b/test/program_test.dart
@@ -888,7 +888,7 @@ void main() {
       expect(assessment.notes, 'Same shoes and surface');
     });
 
-    test('schema six migrates additively through onboarding schema eight', () async {
+    test('schema six migrates additively through daily-input schema nine', () async {
       storedValue = jsonEncode({
         'schemaVersion': 6,
         'days': 4,
@@ -904,7 +904,7 @@ void main() {
       expect(store.athleticSessionIndex, 0);
       expect(store.athleticHistory, isEmpty);
       final migrated = jsonDecode(storedValue!) as Map<String, dynamic>;
-      expect(migrated['schemaVersion'], 8);
+      expect(migrated['schemaVersion'], 9);
       expect(migrated['athleticProgramRun'], 1);
       expect(migrated['athleticHistory'], isEmpty);
       expect(migrated['onboardingVersionSeen'], 0);
diff --git a/test/daily_inputs_lab_test.dart b/test/daily_inputs_lab_test.dart
--- a/test/daily_inputs_lab_test.dart
+++ b/test/daily_inputs_lab_test.dart
@@ -224,6 +224,8 @@ void main() {
     await tester.pumpAndSettle();
 
     expect(find.text('DAILY INPUTS & RECOVERY'), findsOneWidget);
+    await tester.scrollUntilVisible(find.text('Today at a glance'), 300);
+    await tester.pumpAndSettle();
     expect(find.text('Today at a glance'), findsOneWidget);
     expect(find.text('Quick add'), findsOneWidget);
 
diff --git a/test/navigation_widget_test.dart b/test/navigation_widget_test.dart
--- a/test/navigation_widget_test.dart
+++ b/test/navigation_widget_test.dart
@@ -148,6 +148,8 @@ void main() {
     await tester.pumpAndSettle();
 
     expect(find.text('Barbell Bench Press'), findsWidgets);
+    await tester.scrollUntilVisible(find.textContaining('185 lb × 6'), 300);
+    await tester.pumpAndSettle();
     expect(find.textContaining('185 lb × 6'), findsWidgets);
     await tester.tap(find.text('Weight'));
     await tester.pumpAndSettle();

--- a/lib/brand.dart
+++ b/lib/brand.dart
@@ -550,11 +550,16 @@
       ),
       child: child,
     );
-    if (onTap == null) return panel;
-    return InkWell(
-      onTap: onTap,
+    return Material(
+      color: Colors.transparent,
       borderRadius: BorderRadius.circular(22),
-      child: panel,
+      child: onTap == null
+          ? panel
+          : InkWell(
+              onTap: onTap,
+              borderRadius: BorderRadius.circular(22),
+              child: panel,
+            ),
     );
   }
 }
